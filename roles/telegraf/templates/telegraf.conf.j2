# Global tags can be specified here in key="value" format.
[global_tags]

# Configuration for telegraf agent
[agent]
  interval = "10s"
  round_interval = true
  metric_batch_size = 1000
  metric_buffer_limit = 10000
  flush_interval = "10s"

  precision = ""
###############################################################################
#                            OUTPUT PLUGINS                                   #
###############################################################################
[[outputs.influxdb_v2]]
  ## The URLs of the InfluxDB cluster nodes.
  ##
  ## Multiple URLs can be specified for a single cluster, only ONE of the
  ## urls will be written to each interval.
  ##   ex: urls = ["https://us-west-2-1.aws.cloud2.influxdata.com"]
  urls = ["http://localhost:{{ influxdb_port }}"]

  ## API token for authentication.
  token = "{{ telegraf_influxdb_api_token }}"

  ## Organization is the name of the organization you wish to write to; must exist.
  organization = "{{ telegraf_organization }}"

  ## Destination bucket to write into.
  bucket = "{{ telegraf_bucket }}"

###############################################################################
#                            INPUT PLUGINS                                    #
###############################################################################

# HTTP/HTTPS request given an address a method and a timeout
[[inputs.http_response]]
  ## List of urls to query.
  urls = ["https://activism.international/",
  "https://cloud.activism.international/login",
  "https://matrix.activism.international/#/welcome",
  "https://bbb.activism.international/easy/",
  "https://meet.activism.international/",
  "https://pad.activism.international/"]