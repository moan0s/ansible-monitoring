---
- name: "Set up monitoring"
  hosts: "{{ target if target is defined else 'monitoring_servers' }}"
  become: true

  roles:
    - base
    - prometheus
    - grafana
    - node_exporter
    - alertmanager
    - influxdb
    - telegraf
    - influx
    - uptime-kuma